// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id            String            @id @default(cuid())
  email         String            @unique
  name          String
  passwordHash  String
  createdAt     DateTime          @default(now())
  preferences   UserPreferences?
  recordings    Recording[]
}

model UserPreferences {
  id                  String      @id @default(cuid())
  userId              String      @unique
  user                User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  difficulty          Difficulty  @default(EASY)
  backingTrackVolume  Float       @default(0.6)
  samplesVolume       Float       @default(0.8)
  backingTrackMuted   Boolean     @default(false)
  samplesMuted        Boolean     @default(false)
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
}

model Recording {
  id                String    @id @default(cuid())
  userId            String
  user              User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  title             String
  audioFilename     String    // Pointer to the audio file in storage
  eventLogFilename  String    // Pointer to the event log file in storage
  durationSeconds   Float
  fileSizeBytes     Int
  audioMimeType     String
  createdAt         DateTime  @default(now())
}